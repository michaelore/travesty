#include <istream>
#include <map>

using namespace travesty;

int main(int argc, const char* argv[]) {
	if (argc < 2) {
		std::cout << "Usage: travesty n";
		return;
	}
	int nparam = argv[1];
	std::map<std::string, frequencymap> processed;
	bool done = false;
	while (!done) {
		
	}
}
